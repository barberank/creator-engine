{
  "name": "creator-engine",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @creator-engine/mia",
    "build": "npm run build --workspace @creator-engine/mia",
    "lint": "npm run lint --workspace @creator-engine/mia",
    "typecheck": "npm run typecheck --workspace @creator-engine/mia"
  },
  "engines": {
    "node": ">=20"
  }
}
